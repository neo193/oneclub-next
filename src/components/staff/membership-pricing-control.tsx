import Link from "next/link";
import type {MembershipTierSlot} from "@/types/database";
const money=(paise:number)=>new Intl.NumberFormat("en-IN",{style:"currency",currency:"INR",maximumFractionDigits:0}).format(paise/100);
export function MembershipPricingControl({slots}:{slots:MembershipTierSlot[]}){return <section className="membership-pricing-control"><div className="staff-section-heading"><div><p className="eyebrow compact">ADMINISTRATOR ONLY</p><h2>Membership Pricing Control</h2></div><Link className="button button-secondary" href="/staff/tiers">Manage tiers</Link></div><div className="membership-slot-grid">{[1,2].map(position=>{const slot=slots.find(item=>item.display_slot===position);return slot&&!slot.empty?<article className="membership-slot-card" key={position}><p className="eyebrow compact">LIVE SLOT {position}</p><h3>{slot.name}</h3><strong>{money(slot.price_paise||0)}</strong><p className="membership-slot-validity">{slot.validity_months?`${slot.validity_months} months`:"Lifetime"}</p>{slot.allocation_limit&&<small>{slot.allocated||0} of {slot.allocation_limit} allocated</small>}<Link className="button button-secondary membership-slot-edit" href={`/staff/tiers?edit=${slot.id}`}>Edit</Link></article>:<article className="membership-slot-card empty" key={position}><p className="eyebrow compact">LIVE SLOT {position}</p><h3>EMPTY</h3><p>No tier is currently published here.</p><Link className="button button-secondary" href="/staff/tiers">Publish a Draft Tier</Link></article>})}</div></section>}
